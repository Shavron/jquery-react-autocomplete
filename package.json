{
  "name": "jquery-react-autocomplete",
  "version": "0.1.4",
  "private": false,
  "homepage": "https://github.com/Shavron/jquery-react-autocomplete",
  "repository": {
    "type": "git",
    "url": "https://github.com/Shavron/jquery-react-autocomplete.git"
  },
  "main": "./dist/JqueryReactAutocomplete.js",
  "keywords": [
    "react",
    "react autocomplete",
    "react jquery autocomplete",
    "jquery react autocomplete",
    "autocomplete",
    "autocomplete with jquery",
    "web"
  ],
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "dependencies": {
    "jquery": "^3.4.1",
    "jquery-ui-bundle": "^1.12.1-migrate"
  },
  "devDependencies": {
    "@babel/cli": "^7.7.7",
    "@babel/preset-react": "^7.7.4",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.3.0"
  },
  "scripts": {
    "publish": "rm -rf dist && mkdir dist &&  babel src -d dist --copy-files",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
